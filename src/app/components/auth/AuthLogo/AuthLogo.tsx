import React from "react";
import classes from "./AuthLogo.module.css";

const AuthLogo = () => {
    return (
      <div className={classes.authLogo}>
        <h1 className={classes.authLogoTitle}>Экотранс</h1>
      </div>
    );
};

export default AuthLogo;
